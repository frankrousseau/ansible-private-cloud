---

- hosts: all
  sudo: true
  vars_files:
    - vars/defaults.yml
    - vars/{{ 'vagrant' if vagrant is defined else 'user' }}.yml
  roles:
    - common
    - { role: mivok0.users, tags: 'users' }
    # Security roles
    - { role: Ansibles.openssh, tags: 'openssh' }
    # Cloud installation roles
    - { role: cozy, tags: 'cozy' }
    #- { role: Ansibles.nginx, tags: 'nginx' }
